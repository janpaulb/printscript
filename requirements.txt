# Python dependencies
flask>=3.0
python-docx>=1.1
lxml>=5.0
requests>=2.28
gunicorn>=21.2      # production WSGI server (Linux/macOS)

# PDF conversion — pure-Python, no external processes or display required
mammoth>=1.6        # DOCX → HTML (preserves paragraphs, tables, styles)
weasyprint>=61.0    # HTML → PDF  (pydyf backend, no Cairo/Gtk/X11 required)
