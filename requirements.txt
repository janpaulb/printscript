# PrintScript runtime dependencies.
#
# The document pipeline is pure Python: lxml reads the OOXML, PrintScript's own
# renderer turns it into HTML/CSS, and WeasyPrint lays it out as PDF.  There is
# no external converter process, no office suite and no display server.

flask>=3.0,<4
lxml>=5.0
requests>=2.31
weasyprint>=66,<70
gunicorn>=21.2      # production WSGI server
